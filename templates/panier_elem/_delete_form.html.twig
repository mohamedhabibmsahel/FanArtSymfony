<form method="post" action="{{ path('panier_elem_delete', {'idPanier': panier_elem.idPanier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier_elem.idPanier) }}">
    <button class="btn">Delete</button>
</form>

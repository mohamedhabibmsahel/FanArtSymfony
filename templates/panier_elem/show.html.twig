{% extends 'base.html.twig' %}

{% block title %}PanierElem{% endblock %}

{% block body %}
    <h1>PanierElem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPanier</th>
                <td>{{ panier_elem.idPanier }}</td>
            </tr>
            <tr>
                <th>IdProduit</th>
                <td>{{ panier_elem.idProduit }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier_elem.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panier_elem_index') }}">back to list</a>

    <a href="{{ path('panier_elem_edit', {'idPanier': panier_elem.idPanier}) }}">edit</a>

    {{ include('panier_elem/_delete_form.html.twig') }}
{% endblock %}

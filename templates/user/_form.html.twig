{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
{{ form_errors(form) }}

{{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
{{ form_row(form.email) }}

{{ form_row(form.numtel) }}
{{ form_row(form.photo) }}
    {{ form_row(form.type) }}

    <button class="btn btn-primary block full-width m-b">{{ button_label|default('Créer Compte') }}</button>
{{ form_end(form) }}
